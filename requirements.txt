# Core Web Framework
shiny>=0.10.0
shinywidgets>=0.3.0

# Machine Learning & NLP
# transformers must be >=4.51 (AttnLRP via lxt needs the qwen/gemma modeling
# modules) and <5 (5.x drops attention weights under SDPA and breaks lxt).
transformers>=4.51,<5
torch>=2.0.0
tokenizers>=0.15.0
spacy>=3.7.0
scikit-learn>=1.3.0
scipy>=1.10.0
captum>=0.6.0
# AttnLRP (Achtibat et al. 2024) for the LRP cross-validation panel.
lxt>=2.1

# Data Processing
numpy>=1.24.0
pandas>=2.0.0

# Visualization
plotly>=5.18.0
matplotlib>=3.7.0

# Utilities
Pillow>=10.0.0
nltk>=3.8.1
pytest>=9.0.0

# API & Datasets
datasets>=2.14.0
# Used by the user-study interaction log to upload each session to a private
# dataset repo. Arrives transitively via transformers/datasets, but the log
# imports it by name, so declare it.
huggingface_hub>=0.20.0
